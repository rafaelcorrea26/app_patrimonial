import 'package:flutter/material.dart';
import 'qr_code_scanner/PrincipalPage.dart';

void main() {
  // runApp(const MaterialApp(title: "Aplicativo de doação de sangue", home: BarcodePage()));
  runApp(const MaterialApp(title: "Aplicativo de doação de sangue", home: QRViewPage()));
  //runApp(const MaterialApp(title: "Aplicativo de doação de sangue", home: Exemplo()));
}
